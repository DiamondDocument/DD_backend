<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="diamondpick.dd_backend.Dao.lyz.FolderDao">
    <resultMap id="FolderResultMap" type="diamondpick.dd_backend.Entity.lyz.Folder">
        <result column="id" property="folderId"/>
        <result column="f_name" property="folderName"/>
        <result column="c_uid" property="creatorId"/>
        <result column="create_time" property="createTime"/>
        <result column="parent_id" property="parentId"/>
        <result column="self_authority" property="selfAuthority"/>
        <result column="now_authority" property="nowAuthority"/>
    </resultMap>

    <insert id="newFolder">
        insert into `folder`(folder_id, folder_name, creator_id, create_time, parent_id, space_id, self_auth, now_auth)
        values(#{folderId}, #{folderName}, #{creatorId}, #{createTime}, #{parentId}, #{spaceId}, #{selfAuth}, #{nowAuth})
    </insert>

    <!--    <select id="openFolder" resultMap="FolderResultMap">-->
    <!--        SELECT *-->
    <!--        FROM `Folder`-->
    <!--        WHERE `parent_id` = #{folderId}-->
    <!--    </select>-->

    <select id="getParentId">
        select parent_id
        from folder
        where folder_id = #{fileId}
    </select>
</mapper>