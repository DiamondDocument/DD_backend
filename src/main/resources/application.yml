spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:2077/DD_backend
    password: 2002116yy
    username: root
  jpa:
    hibernate:
      ddl-auto: update
mybatis:
  mapper-locations: classpath:Mapper/*.xml
  type-aliases-package: com.example.project.Entity
server:
  port: 2078