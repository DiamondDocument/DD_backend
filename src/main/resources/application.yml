spring:
  datasource:
    url: jdbc:mysql://localhost:2077/dd_backend
    password: 2002116yy
    username: root
  jpa:
    hibernate:
      ddl-auto: update
mybatis:
  mapper-locations: classpath:Mapper/*.xml
  type-aliases-package: diamondpick.dd_backend.Entity
server:
  port: 8080